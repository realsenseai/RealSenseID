set(SRC_DIR ${CMAKE_CURRENT_SOURCE_DIR})

set(HEADERS "${SRC_DIR}/StreamConverter.h" "${SRC_DIR}/MetadataDefines.h" "${SRC_DIR}/RawHelper.h")
set(SOURCES "${SRC_DIR}/StreamConverter.cc" "${SRC_DIR}/RawHelper.cc")

if (UNIX OR ANDROID)
    if(NOT RSID_LIBUVC)
        list(APPEND HEADERS "${SRC_DIR}/V4L2Capture.h" "${SRC_DIR}/JPEGTurboDecoder.h")
        list(APPEND SOURCES "${SRC_DIR}/V4L2Capture.cc" "${SRC_DIR}/JPEGTurboDecoder.cc")
    else()
        list(APPEND HEADERS "${SRC_DIR}/LibUVCCapture.h" "${SRC_DIR}/JPEGTurboDecoder.h")
        list(APPEND SOURCES "${SRC_DIR}/LibUVCCapture.cc" "${SRC_DIR}/JPEGTurboDecoder.cc")
    endif()
elseif (WIN32)
    list(APPEND HEADERS "${SRC_DIR}/MSMFCapture.h" "${SRC_DIR}/JPEGWICDecoder.h")
    list(APPEND SOURCES "${SRC_DIR}/MSMFCapture.cc" "${SRC_DIR}/JPEGWICDecoder.cc")
endif ()

target_sources(${LIBRSID_CPP_TARGET} PRIVATE ${HEADERS} ${SOURCES})
target_include_directories(${LIBRSID_CPP_TARGET} PRIVATE "${SRC_DIR}")
